[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -u -m gunicorn \"app:create_app()\" --bind 0.0.0.0:$PORT --timeout 120 --workers 2 --threads 4 --worker-class gthread"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
